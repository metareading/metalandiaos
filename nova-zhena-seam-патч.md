# SEAM v2 · патч + фрагменти за доер А · v0.1 · 12.09.2026 · доер Б (MET-621 · Модел: Fable 5.1 · дума 12.09)

> Доер Б НЕ пипа `nova-zhena-v2.html`. Тук е всичко, което доер А (или МО3) прилага: 1 команда + 4 фрагмента. Нула ключ в браузъра — шевът е браузър → n8n webhook (публичен URL) → Yespo/HubSpot (ключовете живеят в n8n).

## 1 · Патчът (1 команда · идемпотентен)
```bash
cd /Users/user/metalandiaos
python3 nova-zhena-seam-патч.py nova-zhena-v2.html      # ① заменя <!-- SEAM v2: Yespo segment … --> с <script>шев</script> · ② маха shell-хендлъра (console.log + .done)
grep -c 'nova-zhena-seam.js v0.1.1' nova-zhena-v2.html    # → 1
grep -c 'V2 shell · SEAM v2 Yespo' nova-zhena-v2.html   # → 0
python3 nova-zhena-build-inline.py nova-zhena-v2.html   # inline-копие за Skillplate-паст (шевът вече е вътре · без външен <script src>)
```
Втори пуск → „вече приложен". Ако shell-хендлърът е бил променен от доер А → патчът само предупреждава (шевът го обезсилва сам: `submit` в capture-фаза + `stopImmediatePropagation`).
Проверено на копие: `nova-zhena-tests/_seam-preview.html` (gitignored · `bash nova-zhena-tests/static-checks.sh` го ражда наново).

## 2 · Какво прави шевът (`nova-zhena-seam.js` · v0.1.1 · 157 реда · нула зависимости)
- **submit** → валидация (име ≥ 2 · имейл · телефон ≥ 9 цифри, ако е даден · въпрос при режим `question`) → **POST JSON** към `https://primary-production-110e3.up.railway.app/webhook/new-woman` (живият поток „New-Woman Form" · Yespo `POST /api/v1/contact` → статична група **„Нова Жена форма" (id 202454894)** · HubSpot контакт · известие до екипа · авто-отговор до лийда) → `.fcard.done` (печатът) + събитие `нж:изпратено`.
- **грешка** (HTTP ≠ 2xx · мрежа · таймаут 12 s) → честен ред под бутона: „Не се получи по мрежата. Обадете се на **+359 886 788 857** (и Viber) или пишете на start@metareading.com — отговаряме лично. Може и да опитате пак." · бутонът се връща · събитие `нж:грешка`.
- **товарът** носи легаси полета за СЕГАШНИЯ поток (`spheres` = режим-етикет · `problem` = въпросът · `want_individual_attention` = чекбоксът „пълната програма") + новите (`mode` · `full` · `question` · `source` · `page` · `ts`) — сегашният поток ги подминава, новият ще ги чете.
- **honeypot**: ако има `<input name="company" tabindex="-1" autocomplete="off" style="position:absolute;left:-9999px">` и е попълнено → „успех" без заявка.
- **конфиг без пипане на кода** (ПРЕД шева): `<script>window.НЖ_ШЕВ_КОНФИГ={endpoint:'…/webhook/nova-zhena-sep2026'}</script>` — смяната към новия сегмент е ЕДИН ред.
- **публично**: `НЖшев.режим('question'|'live'|'online'|'unsure')` · `НЖшев.товар()` (какво ще се прати · без изпращане) · `НЖшев.тел()` · `НЖшев.viber()` · `?mode=question` в URL-а отваря формата в режим въпрос.

## 3 · Фрагменти за визуала (доер А владее вида · шевът само чете атрибутите)

> **Интеграция с V2.1 (комит `15965b5` · доер А):** fab-ът вече има режим `q` („Имате въпрос? · Отговаряме лично“) в Проблем·Чудовище·Скала + `<a class="call" href="tel:…">`. Шевът v0.1.1 го разбира БЕЗ нова маркировка: тап на `#fab a[href="#form"]` докато fab е `.q` → `режим('question')` (скрит input `mode=question` · `spheres=„Въпрос“` · `want_individual_attention=false`). Textarea за въпроса (3в) е по избор — без нея валидацията НЕ иска текст, лийдът минава като „Въпрос“ и екипът звъни. Цената е РЕШЕНА (дума 12.09 вечер): **1597 €** → в `let ЦЕНИ={пълна:1527…}` (ред ~1916) се сменя ЕДНО число; стълбата и условието са в §5.
### 3а · Лепкавото CTA · „Заявете място" + „Задайте въпрос" + директно обаждане
```html
<div id="fab">
  <div class="t"><b>Нова Жена · 18–22 сеп</b>Банско или онлайн</div>
  <a class="cta" href="#form">Заявете място</a>
  <a class="cta ghost" href="#form" data-нж-режим="question">Задайте въпрос</a>
  <a class="tel" href="tel:+359886788857" aria-label="Обадете се на +359 886 788 857">📞</a>
</div>
```
Думата (12.09): „това да е отдолу заявете място през повечето време… на места да е задайте въпрос и да има възможност директно да се обадят" → доер А решава КЪДЕ fab-ът показва кой бутон (напр. след секцията „Водещият" → „Задайте въпрос"); `data-нж-режим="question"` е всичко, което шевът иска.

### 3б · Телефон + Viber (footer · „Пишете ми" · fab)
```html
<a href="tel:+359886788857">+359 886 788 857</a> · <a href="viber://chat?number=%2B359886788857">Viber</a>
```
или с атрибути (шевът пълни `href` сам): `<a data-нж-тел>Обадете се</a>` · `<a data-нж-viber>Viber</a>`. На десктоп `viber://` отваря приложението, ако е инсталирано — иначе нищо; текстът с номера остава четим и там.

### 3в · Режим „въпрос" в самата форма (същият шев · поле „въпрос")
```html
<!-- в .choice · четвърти избор (или отделен ред под трите) -->
<label style="margin:0"><input type="radio" name="mode" value="question"><span>Имам<br>въпрос</span></label>
<!-- под телефона · показва се само при режим въпрос -->
<label for="f-q" class="q-wrap" hidden>Вашият въпрос</label>
<textarea id="f-q" name="question" rows="3" class="q-wrap" hidden placeholder="Какво Ви интересува? Отговаряме лично."></textarea>
```
Шевът показва/скрива `.q-wrap` по режима и скрива чекбокса „пълната програма" при въпрос. Без радио „question" → `НЖшев.режим('question')` създава hidden input `mode=question` и пак работи (въпросът тогава е задължителен само ако има textarea).

### 3г · Печатът при въпрос (копи · Митрандир пише текстовете · това е плейсхолдър за мястото)
`.sealed h3` → „Получихме въпроса Ви." · `p` → „Отговаряме лично — обикновено до 24 часа. Ако е спешно: +359 886 788 857."

## 4 · ЧАКА ДУМА (шевът работи и без тях)
1. ★ Сегмент: остава **„Нова Жена форма"** (живият · id 202454894) или нов **„Нова Жена · сеп 2026"**? Новият = нов n8n поток (import-готов JSON: `nova-zhena-tests/n8n-nova-zhena-sep2026.json` · Митрандир го импортира в n8n UI · после `endpoint` = 1 ред).
2. Авто-отговорът на живия поток („Следваща стъпка — запазете Индивидуално внимание (30 мин.)") — пасва ли на страницата, или новият поток праща друг (текст = Митрандир)?
3. Тест-записът от 12.09 (`planetpreneur+nz-seam-test@gmail.com` · „ТЕСТ seam · MET-621") — Yespo контакт 3584518654: DELETE http 200 → GET по id http 200 (Yespo пази записа като „изтрит“/soft; търсене по имейл → „НЯМА контакт“) · erase=true → http 200; HubSpot контакт 248142001217 **чака архивиране от Митрандир** (MCP-инструментът няма delete · в .env няма HubSpot ключ): https://app.hubspot.com/contacts/6402991/record/0-1/248142001217
