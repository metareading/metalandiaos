# Нова Жена V2.1 · тестове · ОТЧЕТ · v0.1 · 12.09.2026 · доер Б (MET-621 · Модел: Fable 5.1 · дума на Митрандир 12.09)

> Всяко число тук е измерено в сесията на 12.09 (Browser pane · bash · n8n/Yespo/HubSpot API). Страницата под тест: `nova-zhena-v2.html` (комит `b79e5e9` + текущите промени на доер А в работното дърво) · шевът: `nova-zhena-seam.js` върху копие `nova-zhena-tests/_seam-preview.html`. iOS Simulator: **няма** (виж §б).

## а · FPS (390×844 · Android UA емулация · dpr 2 · 22 000 зрънца/фигура · `await __мфиг.мери(2000)` · критерий ≥ 45)
| Фигура | FPS | платно | сглобяване |
|---|---|---|---|
| hero (морф часовник→жена) | **60.5** | 390×354 | 1.00 |
| path (съзвездие · 8 етапа) | **60** | 390×338 | 1.00 |
| monster (Спирачката) | **60** | 390×439 | 1.00 |
| scale (скала буря→зора) | **60** | 390×448 | 1.00 |
| sunarc (слънцето по дъгата) | **60** | 390×236 | 0.99 |
Бележка: панелът беше `visibilityState=hidden` за потребителя, но `requestAnimationFrame` течеше (61 кадъра/s измерени преди теста) — числата са живи, не синтетични. `document.hidden=true` → на реален телефон очаквай същия таван 60 (vsync); при < 45 → `БРОЙ` 22 000 → 16 000 (ред 821 в рецептата на V2). Скрипт: `nova-zhena-tests/fps-мери.js` (пейст в конзолата на телефона).

## б · iOS Safari / реален телефон — НЕ Е МЕРЕНО (честна дупка)
`xcrun simctl` липсва — на машината има само CommandLineTools, без пълен Xcode (mcp iOS Simulator `attach` → „needs a full Xcode install“). Заместител: Browser pane 390×844 с мобилен UA (виж §а · §в · §г). За реалния телефон: отвори `http://192.168.8.2:8899/nova-zhena-v2.html` (Mac IP 12.09 · dev-сървърът на :8899) в Safari → конзола (Web Inspector) → пейст `fps-мери.js`. Xcode (App Store) + `sudo xcode-select -s /Applications/Xcode.app/Contents/Developer` отключва симулатора — иска паролата на Митрандир.

## в · reduced-motion · без WebGL
| URL | режим | постери видими | canvas скрити | scrollWidth | console errors |
|---|---|---|---|---|---|
| `?reduced=1` | „постер“ | 5/5 | 5/5 | 390 (=viewport) | 0 |
| `?nogl=1` | „постер“ | 5/5 | 5/5 | 390 | 0 |
Скрийншот: hero показва статичния постер (жената на прозореца) вместо частиците. Дефолтният режим (webgl · без параметри): 0 console errors на 390×844.

## г · Skillplate host-обвивка (емулация · `nova-zhena-tests/host-wrap.html` · нула iframe · страницата се инжектира в `#custom-code` на host-страница с хедър 64 px · контейнер 1140/padding 20/overflow-x hidden · Outfit)
| Мярка | Стойност | Присъда |
|---|---|---|
| canvas-и full-bleed (left/width) | 5/5 → left 0 · width 390 = viewport | ✅ bleed-логиката (clientWidth) работи и в контейнер с padding |
| хоризонтален скрол | scrollWidth 390 = clientWidth | ✅ |
| режим на двигателя | webgl · Спирачката 59.9 FPS вътре в обвивката | ✅ |
| fab (лепкаво CTA) | position fixed · при първи екран bottomGap −60 px (скрит) → след скрол клас `show` · bottomGap 12 px | ✅ по дизайн (появява се след hero) |
| секции инжектирани | 11 | ✅ |
| **body на хоста** | V2-стилът презаписва `body` на host-а: фон `rgb(13,10,8)` + шрифт Inter (хедърът на Skillplate става тъмен/с шрифта на V2) | ⚠️ MED · глобалните селектори на V2 (`body` · `section` · `h2` · `a`…) изтичат върху хедър/футър/бисквитки на Skillplate. Предишните страници (onlineroleplay · НЖ-КУРС) минаха по същия път (цялата страница = custom code), т.е. не е блокер — но доер А да реши: скоп под коренов клас (`.nz2 …`) или съзнателно превземане. |
| `sp-page` фон | прозрачен → тъмният body прозира | (следствие от горното) |
Мерене: `nova-zhena-tests/host-wrap-мери.js` (пейст в конзолата на host-wrap.html). Реалният Skillplate админ не е тестван (без достъп) — чеклист за деня на пейста: (1) пейст на `*.inline.html` · (2) `scrollWidth==clientWidth` на 390 · (3) 5 canvas `left 0` · (4) fab след скрол · (5) хедър/футър на Skillplate четими · (6) `?reduced=1` не е достъпен в Skillplate → `prefers-reduced-motion` на телефона върши същото · (7) конзола 0 грешки · (8) формата: валидация → грешка (изключен Wi-Fi) → реален submit → n8n execution.

## д · Формата · шевът (`nova-zhena-seam.js` v0.1 · копие `_seam-preview.html` · скрипт `seam-тест.js`)
| Тест | Резултат |
|---|---|
| валидация (празна форма) | 2 грешки под бутона: „Как да се обръщаме към Вас?“ · „Проверете имейла — на него отговаряме.“ · **нула заявка** · `done=false` |
| грешка (endpoint → localhost 501) | събитие `нж:грешка · HTTP 501` · честният ред: „Не се получи по мрежата. Обадете се на +359 886 788 857 (и Viber) или пишете на start@metareading.com — отговаряме лично. Може и да опитате пак.“ · бутонът се връща · `done=false` |
| режим „въпрос“ (`НЖшев.режим('question')` без радио) | hidden input създаден · товар: `mode=question` · `spheres=„Въпрос“` · `want_individual_attention=false` |
| **реален submit №1** (18:36:27Z · име „ТЕСТ seam · MET-621 · изтрий“) | `нж:изпратено` · `.fcard.done` · формата `display:none` · печатът `display:block` · n8n execution **122678 success** (6.6 s · UA = емулираният Pixel 8) · известие до екипа OK · авто-отговор до тест-имейла OK · **HubSpot контакт 248142001217 създаден** (`an_keta` „Интерес Нова Жена“ · `n89_8_` „… и сферите Онлайн · Zoom“ = легаси-мапването работи) · ⚠️ Yespo-възелът на ЖИВИЯ поток: **400 FIRST_NAME_INVALID** (тест-името с „·“ и цифри) → контакт без група |
| **реален submit №2** (18:40:57Z · име „Проба Шев“ · същият имейл) | n8n **122681 success** · Yespo `POST /contact` OK → контакт **3584518654** „Проба Шев“ · група **„Нова Жена форма“ (id 202454894)** · HubSpot → PATCH на същия контакт (без дубликат) · известие + авто-отговор OK |
| почистване | Yespo контакт 3584518654 → **DELETE http 200 → GET по id http 200 (Yespo пази записа като „изтрит“/soft; търсене по имейл → „НЯМА контакт“) · erase=true → http 200** · HubSpot 248142001217 → **остава** (MCP-инструментът няма delete; в .env няма HubSpot ключ) → Митрандир архивира: https://app.hubspot.com/contacts/6402991/record/0-1/248142001217 |
Находки за потока (не за шева): (1) n8n `Wait` = 5 s (измерено 5001 ms), не 1 h · (2) FIRST_NAME_INVALID при име със символи/цифри → в import-готовия поток (`n8n-nova-zhena-sep2026.json`) името се санира (`[^\p{L}\s'-]` → празно · fallback „Гост“) и Yespo-възелът е `continueRegularOutput` · (3) телефонът е SMS-канал в Yespo — при празен телефон живият поток праща `"value": ""` (непроверено дали Yespo го приема → в новия поток каналът е условен).
Скрийншоти на състоянията: DOM/JSON-доказани (горе); визуалните кадри след скрол излизаха черни в панела (споделен таб с доер А + скрит панел) — не са приложени, за да не се твърди нищо от черен кадър.

### д2 · Шев v0.2.0 (12.09 късно · след втората дума: варианти без цена · „свържете се с мен“) · локален тест на `_seam-preview.html` · БЕЗ нов реален submit
| Тест | Резултат |
|---|---|
| валидация (празна форма) | 2 грешки · нула заявка · `done=false` |
| `НЖшев.вариант('retreet'→'retreat')` без бутони на страницата | скрит input `variant=retreat` · товар `variant_label` „Само ритрийтът · 18–22 сеп“ · `spheres` „Само ритрийтът · 18–22 сеп · Присъствено · Банско“ · `провери()=[]` |
| грешка-път (фалшив endpoint · variant=course · mode=online) | събитие `нж:грешка · HTTP 501` · честният ред видим · `spheres` „Само курсът · присъствен · Онлайн · Zoom“ |
| режим „въпрос“ + вариант | `mode=question` · `spheres` „Само курсът · присъствен · Въпрос“ · `want_individual_attention=true` |
| събитие `нж:вариант` | `{variant:'online', label:'Онлайн курс'}` |
| чекбокс `contact` (инжектиран САМО в preview-копието · V2.1 още го няма) | `contact=true` · `want_individual_attention=true` · `variant=full` · `грешки=[]` |
| v0.2.1 · вариант → режим (дума: режимът само за пълна/ритрийт) | `course` → режим live · `.choice` скрит · `online` → режим online · скрит · `full`/`retreat` → `.choice` видим · `spheres` „Само курсът · присъствен · Присъствено · Банско“ |
Реален submit за v0.2.0/v0.2.1 НЕ е пращан (товарът е надмножество на v0.1.1; живият n8n поток не валидира формата на тялото · `spheres` е низ). Условието „варианти без цена“ и CTA-блокът са фрагменти за МО3/доер А (`nova-zhena-seam-патч.md` §6), не са в страницата още.

## е · Тегло / диета (bash · cwebp · Pillow · `nova-zhena-tests/diet.py`)
| Вариант | 25 образа | inline-оценка (html 135 162 B + base64 ×1,37) |
|---|---|---|
| сега (q≈80–90 · до 1600 px) | 1 849 874 B | 2 669 489 B (измерено: `nova-zhena-v2.inline.html` = 2 913 859 B) |
| q72 без преоразмеряване | 1 373 078 B | 2 016 278 B |
| ≤ 1000/1400 px + q72 | 1 206 318 B | 1 787 817 B |
| ≤ 900/1280 px + q68 | 1 082 124 B | 1 617 671 B |
| **образите външно** (`https://maitapp.com/img/nz2/…` след push) | 0 в HTML | **135 162 B** |
Присъда: целта ≤ 1,5 MB **не се стига само с re-encode** (най-добрият вариант 1,62 MB · и е видимо по-мек на 2× екрани). ★ Препоръка: (1) образите остават в репото и се сервират от GitHub Pages (`maitapp.com/img/nz2/`) → пейстът е 135 KB, образите се кешират · иска **push** (нула push без дума → ЧАКА ДУМА); (2) до пуша: `≤ 1000/1400 px + q72` (1,79 MB · −33 %) като междинна диета. Най-тежките 5: `vodach.webp` 167 904 · `transition.webp` 136 802 · `dawn-wide.webp` 121 614 · `concert.webp` 115 846 · `hero-now.webp` 100 138 B.

## ж · „Различни тестове“ = A/B → план, не билд · `nova-zhena-tests/ab-план.md` · ЧАКА ДУМА

## Статични проверки (`bash nova-zhena-tests/static-checks.sh` · 12.09)
забранената дума (канон: „предложение“) → 0 във всички NZ-файлове · `<iframe>` в v2 → 0 · seam.js parse OK · цени.json valid (1597 € · капаро 543) · патч на копие OK (маркер 1 · shell махнат 1 · стар shell 0).

## Какво НЕ е направено (честно)
- iOS Safari / реален телефон (§б) · реалният Skillplate админ (§г · без достъп) · A/B (§ж · план) · точното видео (виж `nova-zhena-видео-карта.md` · ЧАКА ДУМА) · HubSpot тест-контакт (остава · линк горе) · десктоп-обвивка (5 % трафик · само 390).
